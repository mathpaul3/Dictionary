<!DOCTYPE html>
<html lang="en">
  <%- include("../header"); %>

  <% if (user.isdark == "dark") { %>
    <body style="background-color: rgb(55, 55, 55);">
  <% } else { %>
    <body>
  <% } %>
    <%- include("../menu"); %> <%- include("./searchbar"); %> <%if ( user.isdark ==
    "dark") { if ( noresult ) { %>
    <p style="text-align: center; font-size: 20px; color: ivory;">
      <span style="color: orangered;">"<%=wordparam%>"</span>에 대한 검색 결과가
      없습니다.
    </p>
    <p style="text-align: center; font-size: 15px; color: ivory;">
      단어를 다시 한 번 확인해주세요.
    </p>
    <% } else { result.forEach(word => { %>
    <div
      class="card card-body"
      style="
        margin: 10px 10%;
        padding: 0;
        padding-left: 20px;
        padding-top: 10px;
        background-color: rgb(105, 105, 105);
      "
    >
      <h2 class="display-5" style="color: ivory;">
        <a style="color: ivory;" href="../<%=lang%>/<%=word.word%>/<%=word._id%>"
          ><%=word.word%></a
        >
      </h2>
      <p class="lead" style="color: ivory;"><%=word.meaning%></p>
    </div>
    <% }); } } else { if ( noresult ) { %>
    <p style="text-align: center; font-size: 20px;">
      <span style="color: crimson;">"<%=wordparam%>"</span>에 대한 검색 결과가
      없습니다.
    </p>
    <p style="text-align: center; font-size: 15px;">
      단어를 다시 한 번 확인해주세요.
    </p>
    <%} else { result.forEach(word => { %>
    <div
      class="card card-body"
      style="
        margin: 10px 10%;
        padding: 0;
        padding-left: 20px;
        padding-top: 10px;
      "
    >
      <h2 class="display-5">
        <a href="../<%=lang%>/<%=word.word%>/<%=word._id%>" style="color: black;"
          ><%=word.word%></a
        >
      </h2>
      <p class="lead"><%=word.meaning%></p>
    </div>
    <% }); } } %> <%-include("../footer"); %> <%- include("../searchScript"); %> <%-include("../darkScript");%>
  </body>
</html>
